## Status

We downloaded ollama model to use as a local model to send requests. 

## Instructions

Check that everything is configured to: 

1) launch backend in a local server running inside the container (can be a container running in my laptop or a github codespace container)
2) Set a local Ollama model to answer requests. Check that it has been downloaded and location 
3) launch a web-ony front (without Electron)
4) have everything running inside a codespadce container, with proper ports setup. Always use the ports indicated in the README and 
documentas, if they are not free kill the processes instead using other ports)
5) create tests to check that everything is working. The consist of sending a "2+2" promt to the ollama model and getting a proper answer like "4"
6) update the section "## Quick Start" below with the required steps as to properly reproduce the setup and launching of backend, frontend and all involved services inside a container. Include tests to check that everythin is working, including a "2+2"="4" test. 
7) update the section "## Sequential Diagram" below with a sequential diagram illustrating all the components and services and how they wotk together

## Quick Start

## Sequential Diagram

